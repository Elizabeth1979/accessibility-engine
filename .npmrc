# pnpm settings for the AEE workspace.
# Peer deps (e.g. @playwright/test) are auto-installed.
auto-install-peers=true
# Browsers are not needed for the scaffold; install with
# PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 to skip the download.
