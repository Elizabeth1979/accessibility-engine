# JSON Schemas (generated)

These files are **generated** from the zod schemas in `@aee/core` — do not edit by hand.

Regenerate with:

```bash
pnpm gen:schemas
```

CI verifies they are in sync. Each schema carries a `schemaVersion`; bump it in `@aee/core` when a contract changes.
