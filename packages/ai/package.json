{
  "name": "@aee/ai",
  "version": "0.0.0",
  "type": "module",
  "description": "AI judgment + conversational explain() + fix drafting. Sees captured evidence ONLY; never a driver or the live page.",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "pretest": "tsc -b",
    "test": "node --test dist"
  },
  "dependencies": {
    "@aee/core": "workspace:*",
    "@anthropic-ai/sdk": "^0.105.0",
    "zod": "^3.24.1"
  }
}
