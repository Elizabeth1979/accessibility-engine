{
  "name": "@aee/engine",
  "version": "0.0.0",
  "type": "module",
  "description": "Orchestrator: composes capture + judges + AI + reporter into investigate(). The only package that may touch both a driver and the judges.",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./test": {
      "types": "./dist/fixture.d.ts",
      "import": "./dist/fixture.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "pretest": "tsc -b",
    "test": "node --test dist"
  },
  "dependencies": {
    "@aee/core": "workspace:*",
    "@aee/playwright": "workspace:*",
    "@aee/judges": "workspace:*",
    "@aee/ai": "workspace:*",
    "@aee/reporter": "workspace:*"
  },
  "peerDependencies": {
    "@playwright/test": ">=1.40.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
